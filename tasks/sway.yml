- name: Install applications used for sway setup 1
  become: true
  ansible.builtin.package:
    name:
    - sway
    - fuzzel
    - rofi
    - wlogout
    - fira-code-fonts
  tags:
    - install
    - sway
    - productivity

- name: Install applications used for sway setup 2
  become: true
  ansible.builtin.package:
    name: ["waybar", "wl-clipboard", "mpc", "mpv", "grim", "slurp"]
  tags:
    - install
    - sway
    - productivity

- name: Install applications used for sway setup 3
  become: true
  ansible.builtin.package:
    name: ["light", "mako", "ncmpcpp", "pavucontrol", "ranger", "swaylock", "swaybg"]
  tags:
    - install
    - sway
    - productivity
